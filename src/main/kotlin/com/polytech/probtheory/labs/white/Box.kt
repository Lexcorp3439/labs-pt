package com.polytech.probtheory.labs.white

import com.polytech.probtheory.model.Info

class Box(val count: Int): Info {

    override fun toString(): String {
        return "Box(count=$count)"
    }
}